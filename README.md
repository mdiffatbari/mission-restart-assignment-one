1. What is the difference between null and undefined?

Ans: null is a value assigned by the developer and undefined is variable declared bu not assigned value.

2) What is the use of the map() function in JavaScript? How is it different from forEach()?

Ans: map() is used to create a new array from existing array by loop throw each element. If return needed have to use map() else forEach().

3) What is the difference between == and ===?

Ans: == compare only value but === compare value and type.

4) What is the significance of async/await in fetching API data?

Ans: async make a function to return promise $ await pause the code until promise resolved.

5) Explain the concept of Scope in JavaScript (Global, Function, Block).

Ans:

Global Scope: variable declared outside any function or block.
Function Scope: 
Block Scope: